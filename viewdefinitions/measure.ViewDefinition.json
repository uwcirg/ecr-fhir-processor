{
  "resourceType": "ViewDefinition",
  "id": "measure",
  "name": "measure_view",
  "status": "active",
  "resource": "Measure",
  "description": "Flattened analytics view: one row per first-class Measure persisted by this project. Empty-until-loaded: no Measure resource is persisted by this project today (fixtures only reference APHL Measure canonicals), so this view materializes and returns zero rows until Measure resources are loaded — success, not failure (research.md R5). Default column set is an informed, reviewable default (FR-002), not a finalized analytics-team contract.",
  "select": [
    {
      "column": [
        { "name": "id", "path": "getResourceKey()", "type": "string" },
        { "name": "url", "path": "url", "type": "string" },
        { "name": "version", "path": "version", "type": "string" },
        { "name": "name", "path": "name", "type": "string" },
        { "name": "title", "path": "title", "type": "string" },
        { "name": "status", "path": "status", "type": "code" },
        { "name": "scoring", "path": "scoring.coding.first().code", "type": "code" },
        { "name": "identifier", "path": "identifier.first().value", "type": "string" },
        { "name": "cms_measure", "path": "meta.tag.where(system = 'https://uwcirg.github.io/ecr-fhir-processor/CodeSystem/cms-measure').code.first()", "type": "code" }
      ]
    }
  ],
  "where": [
    {
      "path": "meta.tag.where(system = 'https://uwcirg.github.io/ecr-fhir-processor/CodeSystem/processed-by' and code = 'ecr-fhir-processor').exists()",
      "description": "Scope the view to Measures persisted by this project's processor (FR-005): match this processor's provenance tag (processed-by = ecr-fhir-processor), version-agnostic. Excludes unrelated Measure resources on the same server."
    }
  ]
}
